import { css } from "@/styled-system/css";

export const sourceCodeStyle = css.raw({
  fontSize: "0.9em",
  fontFamily: "'Source Code Pro', monospace",
  whiteSpace: "pre-wrap",
});

export const linkColor = css.raw({
  color: "#0048d6",
});
